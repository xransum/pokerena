version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    install:
      - pip install uv
      - uv sync --group dev
    build:
      html:
        - uv run mkdocs build --site-dir $READTHEDOCS_OUTPUT/html

