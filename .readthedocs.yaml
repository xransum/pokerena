version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.13"
  commands:
    install:
      - pip install uv
      - uv sync --group dev
    build:
      - uv run mkdocs build --site-dir $READTHEDOCS_OUTPUT/html

